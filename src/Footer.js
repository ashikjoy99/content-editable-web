import { Stack, Typography } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Stack sx={{ backgroundColor: "#5E7360" }}>
      <Typography>StoreName</Typography>
      <Typography>SignIn</Typography>
      <Typography>StoreName</Typography>
      <Typography>SignIn</Typography>
      <Typography>StoreName</Typography>
      <Typography>SignIn</Typography>
    </Stack>
  );
};

export default Footer;
